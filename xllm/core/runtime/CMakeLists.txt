include(cc_library)
include(cc_binary)

cc_library(
  NAME
    task_pipeline_model_input_layout
  HDRS
    task_pipeline/model_input_layout.h
  SRCS
    task_pipeline/model_input_layout.cpp
  DEPS
    glog::glog
    nlohmann_json::nlohmann_json
    proto::xllm_proto
)

if(USE_NPU)
  cc_library(
    NAME
      task_pipeline_token_result_storage
    HDRS
      task_pipeline/token_result_storage.h
    SRCS
      task_pipeline/token_result_storage.cpp
    DEPS
      :platform
  )

  cc_library(
    NAME
      task_pipeline_sampling_input_binding
    HDRS
      task_pipeline/sampling_input_binding.h
    SRCS
      task_pipeline/sampling_input_binding.cpp
    DEPS
      :platform
  )

  cc_library(
    NAME
      task_pipeline_model_input_binding
    HDRS
      task_pipeline/model_input_binding.h
    SRCS
      task_pipeline/model_input_binding.cpp
    DEPS
      :task_pipeline_model_input_preparer
      :model
  )

  cc_library(
    NAME
      task_pipeline_model_input_preparer
    HDRS
      task_pipeline/model_input_preparer.h
    SRCS
      task_pipeline/model_input_preparer.cpp
    DEPS
      :task_pipeline_model_input_storage
      :platform
  )

  cc_library(
    NAME
      task_pipeline_model_input_storage
    HDRS
      task_pipeline/model_input_storage.h
    SRCS
      task_pipeline/model_input_storage.cpp
    DEPS
      :task_pipeline_model_input_layout
      torch
  )
endif()

cc_library(
  NAME
    runtime
  HDRS
    options.h
    decode_graph_bucket.h
    forward_params.h
    dit_forward_params.h
    params_utils.h
    executor.h
    $<$<BOOL:${USE_NPU}>:task_pipeline/llm_task_program.h>
    $<$<BOOL:${USE_NPU}>:task_pipeline/task_execution_pipeline.h>
    executor_impl.h
    executor_impl_factory.h
    vlm_executor_impl.h
    base_executor_impl.h
    dit_executor.h
    $<$<BOOL:${USE_NPU}>:acl_graph_bucket_policy.h>
    $<$<BOOL:${USE_NPU}>:acl_graph_executor_impl.h>
    $<$<BOOL:${USE_NPU}>:acl_graph_persistent_param.h>
    $<$<BOOL:${USE_XLITE}>:xlite_executor_impl.h>
    $<$<BOOL:${USE_MLU}>:mlu_graph_executor_impl.h>
    $<$<BOOL:${USE_CUDA}>:cuda_graph_executor_impl.h>
    $<$<BOOL:${USE_MUSA}>:musa_graph_executor_impl.h>
    py_executor_impl.h
    py_attention_metadata.h
    $<$<BOOL:${USE_DCU}>:dcu_graph_executor_impl.h>
    worker.h
    worker_impl.h
    llm_worker_impl.h
    vlm_worker_impl.h
    dit_worker_impl.h
    embed_worker_impl.h
    embed_vlm_worker_impl.h
    rec_worker_impl.h
    mm_embed_vlm_worker_impl.h
    worker_client.h
    xservice_client.h
    speculative_worker_impl.h
    mtp_worker_impl.h
    suffix_worker_impl.h
    eagle3_worker_impl.h
    dflash_worker_impl.h
    dflash2_worker_impl.h
    dspark_worker_impl.h
    forward_shared_memory_manager.h
    worker_rendezvous.h
  SRCS
    decode_graph_bucket.cpp
    executor.cpp
    $<$<BOOL:${USE_NPU}>:task_pipeline/llm_task_program.cpp>
    $<$<BOOL:${USE_NPU}>:task_pipeline/task_execution_pipeline.cpp>
    executor_impl_factory.cpp
    base_executor_impl.cpp
    vlm_executor_impl.cpp
    dit_executor.cpp
    $<$<BOOL:${USE_NPU}>:acl_graph_executor_impl.cpp>
    $<$<BOOL:${USE_NPU}>:acl_graph_persistent_param.cpp>
    $<$<BOOL:${USE_XLITE}>:xlite_executor_impl.cpp>
    $<$<BOOL:${USE_MLU}>:mlu_graph_executor_impl.cpp>
    $<$<BOOL:${USE_CUDA}>:cuda_graph_executor_impl.cpp>
    $<$<BOOL:${USE_MUSA}>:musa_graph_executor_impl.cpp>
    py_executor_impl.cpp
    py_attention_metadata.cpp
    $<$<BOOL:${USE_DCU}>:dcu_graph_executor_impl.cpp>
    worker.cpp
    worker_impl.cpp
    worker_rendezvous.cpp
    llm_worker_impl.cpp
    vlm_worker_impl.cpp
    dit_worker_impl.cpp
    embed_worker_impl.cpp
    embed_vlm_worker_impl.cpp
    rec_worker_impl.cpp
    mm_embed_vlm_worker_impl.cpp
    worker_client.cpp
    xservice_client.cpp
    params_utils.cpp
    speculative_worker_impl.cpp
    mtp_worker_impl.cpp
    suffix_worker_impl.cpp
    eagle3_worker_impl.cpp
    dflash_worker_impl.cpp
    dflash2_worker_impl.cpp
    dspark_worker_impl.cpp
    forward_shared_memory_manager.cpp
  DEPS
    :task_pipeline_model_input_layout
    $<$<BOOL:${USE_NPU}>:task_pipeline_sampling_input_binding>
    $<$<BOOL:${USE_NPU}>:task_pipeline_token_result_storage>
    $<$<BOOL:${USE_NPU}>:task_pipeline_model_input_binding>
    $<$<BOOL:${USE_NPU}>:task_pipeline_model_input_storage>
    $<$<BOOL:${USE_NPU}>:task_pipeline_model_input_preparer>
    torch
    $<$<BOOL:${USE_NPU}>:torch_npu>
    :common
    :platform
    :model_context
    :request
    :speculative
    :encoder_cache
    :state_dict
    :dit_cache
    :model
    :parallel_state
    :kv_cache
    :kv_cache_estimation
    :kv_cache_transfer
    :linear_state_restore
    :kv_transfer_completion
    :models
    :sampler
    $<$<BOOL:${USE_NPU}>:prepared_sampler>
    :tokenizer
    :model_loader
    :util
    :worker_service
    :xllm_server
    $<$<BOOL:${USE_NPU}>:xllm_ops>
    $<$<BOOL:${USE_NPU}>:atb_customize>
    $<$<BOOL:${USE_NPU}>:platform_npu>
    :eplb
    glog::glog
    Folly::folly
    absl::strings
    absl::synchronization
    absl::flat_hash_map
    proto::xllm_proto
    $<$<BOOL:${USE_MLU}>:torch_mlu>
    $<$<BOOL:${USE_MUSA}>:torch_musa>
    $<$<BOOL:${USE_DCU}>:hip::host>
)

if(USE_XLITE)
  xllm_link_xlite(runtime)
endif()

if(USE_NPU)
  target_link_libraries(runtime PUBLIC nnopbase)
endif()
